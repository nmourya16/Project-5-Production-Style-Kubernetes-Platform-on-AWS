apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project5-app-pvc
  namespace: project5

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: gp2

  resources:
    requests:
      storage: 1Gi